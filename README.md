Key decisions you should be aware of:

Hosted-agent flow instead of the get-started-with-ai-agents repo. I verified the repo is still a Container Apps web-app pattern, while everything else in your TOC (per-agent Entra ID, versioning/rollback, rai_config guardrails, Voice Live by agent ID, azd ai agent commands) only exists in the hosted-agents GA flow (azd ext install microsoft.foundry + azd ai agent init). Exercise 2 uses the hosted-agent template as the backbone and mentions the repo as the alternative pattern in a note. If you want the repo's azd up flow instead, say so and I'll rework Exercise 2, but Modules 3 and 5 would lose their technical basis.
Continuity across exercises: one Foundry project (agent-project) and resource (foundry-SUFFIX) created in Exercise 1 is reused everywhere; the portal prompt agent serves the Voice Live channel while the hosted agent (contoso-hosted-agent) carries the identity, guardrail, versioning, and tracing story.
All commands verified against July 2026 docs — azd ai agent invoke/show/monitor, az rest version-selector rollback with the Foundry-Features header, rai_config ARM policy IDs, the azure-ai-voicelive agent-mode connection, and the portal evaluation wizard. Where the live portal UI couldn't be verified (portal blade names, template variable names, Voice Live region support, SDK preview class names), I placed explicit > **Note (Author review needed):** flags — there are 9 across the five files.
Formatting self-check passed: every step uses 1. numbering, UI elements are bolded with (n) markers, image placeholders follow Ex{N}-Task{T}-Step{S}.png, inject tokens cover credentials and suffixes, validation blocks follow every hands-on task, zero em/en dashes, and Review bullets match Objectives in past tense. Exercise 5 ends with the lab-complete closing.

One housekeeping note: your old reference files (Exercise-01.md, the Voice Live draft, Search-History.md, TOC.md) are untouched. The old Exercise-01.md sits alongside the new Exercise-1.md, so you may want to archive the old drafts before handing this to the lab team.